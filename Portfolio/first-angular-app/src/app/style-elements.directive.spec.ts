import { StyleElementsDirective } from './style-elements.directive';

describe('StyleElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleElementsDirective();
    expect(directive).toBeTruthy();
  });
});
